# BIONEMO

## Setup
Run `docker.sh` to create a docker container. Peek inside the script to change certain options (i.e. pulling the latest image).

Inside the container, you have to install [NGC](https://org.ngc.nvidia.com/setup/installers/cli) according to instructions for AMD64 Linux.
